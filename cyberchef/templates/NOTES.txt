CyberChef has been deployed!

{{- if .Values.ingress.enabled }}
Visit https://{{ .Values.ingress.host }} to use it.
{{- else }}
It is only reachable inside the cluster on port {{ .Values.service.port }} (service type: {{ .Values.service.type }}).
Enable ingress in values.yaml to expose it externally.
{{- end }}
