apiVersion: v1
kind: Service
metadata:
  name: {{ include "snake.fullname" . }}
  labels:
    {{- include "snake.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "snake.selectorLabels" . | nindent 4 }}
